import styled from 'styled-components';

import {UserIcon} from '~/assets';

export const WorkspaceWrapper = styled.div`
  background: #2d4071;
  padding: 6px 16px;
  color: #ffffff;
  margin-bottom: 13px;
  display: flex;
  align-items: center;
`;

export const WorkspaceTitle = styled.p`
  color: #ffffff;
  margin-left: 8px;
`;

export const WorkspaceImage = styled(UserIcon)``;
