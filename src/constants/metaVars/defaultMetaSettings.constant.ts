import DashboardMetaSettings from './dashboardMetaSettings.constant';

const DefaultMetaSettings = {
  name: 'BordioBoard',
  domain: 'https://bordio.com',
  description: DashboardMetaSettings.description,
} as const;

export default DefaultMetaSettings;
